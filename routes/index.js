import { paths } from "../Utils/constants";

export const publicRoutes = [
    {
      name: 'resetPassword',
      path: paths.RESET_PASSWORD,
    },
    {
      path: paths.SIGN_IN,
      name:'login'

    }
  ];
  